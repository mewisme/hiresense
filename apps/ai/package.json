{
  "name": "@hiresense/ai",
  "version": "0.0.1",
  "description": "HireSense AI service",
  "author": "Nguyễn Mậu Minh",
  "private": true,
  "license": "Apache-2.0",
  "scripts": {
    "compile:all": "uv run python -m compileall app",
    "dev": "uv run uvicorn app.main:app --reload --port 8000",
    "test": "uv run python -m unittest discover -s test -p \"test_*.py\""
  }
}